{
  "name": "sgci",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=@sgci/web",
    "dev:api": "npm run start:dev --workspace=@sgci/api"
  }
}